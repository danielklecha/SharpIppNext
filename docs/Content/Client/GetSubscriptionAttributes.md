# Get-Subscription-Attributes Example

```csharp
var client = new SharpIppClient();
var request = new GetSubscriptionAttributesRequest();
var response = await client.GetSubscriptionAttributesAsync(request);
```
