# Create-Resource-Subscriptions Example

```csharp
var client = new SharpIppClient();
var request = new CreateResourceSubscriptionsRequest();
var response = await client.CreateResourceSubscriptionsAsync(request);
```
