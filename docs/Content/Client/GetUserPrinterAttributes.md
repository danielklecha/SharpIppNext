# Get-User-Printer-Attributes Example

```csharp
var client = new SharpIppClient();
var request = new GetUserPrinterAttributesRequest();
var response = await client.GetUserPrinterAttributesAsync(request);
```
